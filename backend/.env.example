# Copy to .env and set values. Database (PostgreSQL)
DB_NAME=flow_reports_db
DB_USER=flow_reports_user
DB_PASSWORD=flow_reports_password
DB_HOST=localhost
DB_PORT=5432

# Optional: cache (Power BI–style refresh). If unset, uses in-memory cache.
# REDIS_URL=redis://localhost:6379/0
# QUERY_CACHE_TIMEOUT=300
